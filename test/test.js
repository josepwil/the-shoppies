const { assert } = require('chai');


describe('lookUp', function() {
  it('should return results from OMDB if they exist', function() {
    
  });

  it('should show list the title, year of release and contain a button to nominate that film', function() {

  });

  it('should update the result list when the search terms are updated', function() {

  });
});

describe('nomimate', function() {
  it('should add movie to nominated list', function() {
    
  });

  it('should not allow a nominated movie to be re-nominated', function() {

  });

  it('should display a banner when the user has 5 nominations', function() {

  });

  it('should hide the banner if the user removes a nomination', function() {

  });
});

describe('removeNomination', function() {
  it('should remove movie from nominated list', function() {
    
  });

});


